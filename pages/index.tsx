import React from 'react';
import App from '../components/app';
import Layout from '../components/layout';

const Home: React.FC = () => {
  return (
    <Layout>
      <App />
    </Layout>
  );
};

export default Home;
