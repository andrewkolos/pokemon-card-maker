import React from 'react';
import App from '../src/components/app';
import Layout from '../src/components/layout';

const Home: React.FC = () => {
  return (
    <Layout>
      <App />
    </Layout>
  );
};

export default Home;
